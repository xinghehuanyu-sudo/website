---
title: 我用纯前端做了两个“给自己用”的小工具
date: 2026-08-18 11:00:00
description: 我用纯前端做了一个旅行 AA 结算器和一条学习日志时间轴，也重新体会了“为自己做工具”的乐趣。
tags:
  - 网页
  - 前端
  - 学习计划
categories:
  - 日常
---
> 没有数据库，也没有登录注册，打开浏览器就能使用。这大概是前端最朴素、也最自由的样子。

---

## 起因

生活中总有一些微小但具体的需求：专门安装一个 App 显得太重，用 Excel 又不够直观。比如和朋友旅行回来后算账，或者记录每天真实发生的学习时间线。

市面上当然不缺类似工具，但用起来总觉得“差那么一点”：有的夹着广告，有的必须注册账号，还有的功能过于臃肿，反而找不到真正需要的入口。

于是我问自己：既然会写前端，为什么不干脆为自己做一个？

就这样，两个轻量的小工具慢慢成形了。

---

## 项目一：多人旅行 AA 结算器

### 解决什么问题？

三五好友一起出游，你请了晚饭，他垫了门票，我付了打车费……旅程结束后，最头疼的往往不是整理照片，而是算清楚谁该给谁多少钱。

手动算容易出错，用群收款又不够灵活（比如有的人只参与了其中一顿饭）。

### 核心逻辑

每个人有一个「已垫付金额」和一个「应承担金额」，两者相减得到净额：

- **净额 > 0**：别人欠你钱
- **净额 < 0**：你欠别人钱

接下来用贪心思路完成转账匹配：每轮找出欠款最多的人和应收最多的人，按双方金额的较小值完成一次转账，再重复这个过程，直到所有净额归零。最终得到一组清晰、尽量精简的转账建议。

### 功能清单

- 参与人管理、消费录入（金额、垫付人、分摊人、标签、备注）
- AA 核心结算 + 清晰的转账建议
- 按标签统计消费，配一个饼图
- 使用 `localStorage` 本地保存，支持 JSON 导入和导出
- 结算报告一键导出 PNG
- 移动端采用卡片化布局，并增大关键操作区域


---

## 项目二：学习日志时间轴

### 解决什么问题？

备考或自学时，我经常想知道：今天到底学了几个小时？哪些时段原本有计划却没有完成？又有哪些投入是计划之外的额外学习？

传统待办清单通常只记录“做了没有”，却很难回答“什么时候做的”和“持续了多久”。

### 交互设计

页面采用双栏结构：左侧是**计划区**，右侧是**记录区**，中间用一条小时轴建立时间参照。

添加事件的流程尽量保持直观：

1. 点「在时间轴选时间」
2. 鼠标移动，会看到一个吸附光标和当前时间提示
3. 点击确定起点（出现橙色标记）
4. 再点击确定终点
5. 弹窗里填学科和内容，保存

事件会以紧凑的箭头卡片显示，支持双击编辑和右键删除。导出的 PNG 使用纯白背景，方便直接插入笔记或复盘文档。

### 其他功能

- **专注计时**：正计时/倒计时，结束后自动写入记录区
- **复习任务**：根据学习记录生成复习计划，按今天/本周/本月筛选
- **撤销/重做**：`Ctrl+Z` / `Ctrl+Y`
- **可调设置**：起始小时、小时高度、双栏占比、学科颜色……

---
## 共同的设计原则

这两个项目有几个共同点，也是我给自己定的约束：

| 原则 | 说明 |
|------|------|
| **零后端** | 没有服务器、没有数据库、没有 API |
| **零登录** | 打开就用，不收集任何信息 |
| **数据在本地** | `localStorage` 保存，JSON 导入/导出做备份 |
| **零构建** | 不需要 `npm install`，也不依赖打包工具，浏览器直接打开 |
| **结构轻量** | 每个项目只由 `index.html`、`styles.css` 和 `app.js` 组成 |

技术上并没有刻意追求复杂：AA 结算依赖一段贪心逻辑，时间轴主要是 DOM 计算与事件监听，饼图则交给 Canvas 绘制。对这类个人工具来说，清晰、稳定、够用，本身就是合适的架构。

---

## 一点感想

写这两个工具的过程中，我最大的体会是：

**为自己做的工具，不必一开始就追求“通用性”。**

它不需要用户系统，也不必承担多租户、并发和复杂权限这些产品级负担。我更愿意先问一个简单的问题：“这个功能，我自己真的会用吗？”

用得上就加，用不上就删。这种看似“自私”的开发方式，反而让产品始终保持轻巧。

当然，纯前端也有代价：数据默认只存在当前浏览器里，换设备或清理浏览器数据都可能导致记录丢失。因此，我为两个项目都加入了 JSON 导入和导出，用最低的成本保留手动备份与迁移能力。

---

## 最后

如果你也有类似的小需求，不妨亲手做一个。无需先搭建复杂框架，也不必先想清楚所有功能；从一个 HTML 页面开始，就已经足够。

或许，前端最初吸引人的地方正是如此：写下一点代码，刷新浏览器，一个真正能用的东西就出现了。

---

*两个项目的结构都很简单：`index.html` 负责内容结构，`styles.css` 负责视觉样式，`app.js` 负责交互逻辑。无需构建，直接在浏览器中打开即可运行。*
